<h2>Sign up</h2>

<%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
  <%= render "devise/shared/error_messages", resource: resource %>

  <div class="field">
    <%= f.label "Name" %><br />
    <em>Searches the Cynnabar Order of Precedence. If you don't have an entry there, just enter your full mundane or SCA name.</em><br />
      <%= f.hidden_field :recipient_id %>
      <%= f.autocomplete_field :name,
        autocomplete_recipient_name_recipients_path, 
        :id_element => '#user_recipient_id',
        :"data-autocomplete-label" => "No matches found!",
        :size => 75 %> <br />
  </div>

  <div class="field">
    <%= f.label :email %><br />
    <em>Your e-mail address will never be shown on the web site or given to anyone else for any reason. It will only be used to contact you about your account. If you choose to have a public profile on the Cynnabar web site, users can contact you at the email you provide here via a web form.</em><br />

    <%= f.email_field :email, autofocus: true %>
  </div>

  <div class="field">
    <%= f.label :password %>
    <% if @minimum_password_length %>
    <em>(<%= @minimum_password_length %> characters minimum)</em>
    <% end %><br />
    <%= f.password_field :password, autocomplete: "off" %>
  </div>

  <div class="field">
    <%= f.label :password_confirmation %><br />
    <%= f.password_field :password_confirmation, autocomplete: "off" %>
  </div>

  <div class="actions">
    <%= f.submit "Sign up" %>
  </div>

  <p> <%= recaptcha_tags %> </p>

  <b>Your account must be approved before you can log in.</b>

<% end %>

<%= render "devise/shared/links" %>
