<table width="100%">
  <tbody>
    <tr>
      <td>
        <img src="<%= office.image %>" width="70" height="70" alt="<%= office.name %>" border="0">
      </td>
      <td>
        <font size="+1">Office of the</font> <br><font size="+2"> <b><%= office.name %></b> </font>
      </td>

      <td align="right">

        <b><%= office.officer_name %></b>

        <br>

        <b> <a href="mailto:<%= office.email%>"><%= office.email%></a> </b>

        <br>
      </td>
    </tr>
  </tbody>
</table>
